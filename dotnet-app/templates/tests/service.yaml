apiVersion: v1
kind: Service
metadata:
  name: {{ include "dotnet-app.fullname" . }}
spec:
  selector:
    app: {{ include "dotnet-app.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 5000
  type: {{ .Values.service.type }}
