apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dot.fullname" . }}-test-connection"
  labels:
    {{- include "dot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
